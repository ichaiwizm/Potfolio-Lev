/**
 * Window system constants
 */

/** Maximum HTML content size in bytes (50KB) */
export const MAX_HTML_SIZE = 50000;

/** Maximum number of windows that can be open simultaneously */
export const MAX_WINDOWS = 10;

/** Minimum window width in pixels */
export const MIN_WIDTH = 100;

/** Maximum window width in pixels */
export const MAX_WIDTH = 2000;

/** Minimum window height in pixels */
export const MIN_HEIGHT = 100;

/** Maximum window height in pixels */
export const MAX_HEIGHT = 1500;
